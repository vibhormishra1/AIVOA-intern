import {useEffect} from 'react'; import {useDispatch,useSelector} from 'react-redux'; import {fetchComplaints} from '../store/slices/dashboardSlice'; export default function Dashboard(){const d=useDispatch();const items=useSelector(s=>s.dashboard.items);useEffect(()=>{d(fetchComplaints())},[d]);return <div className="dashboard"><div className="eyebrow">QUALITY MANAGEMENT SYSTEM</div><h1>Complaint dashboard</h1><div className="table"><div className="tr th"><span>ID</span><span>Customer</span><span>Product</span><span>Severity</span><span>Status</span></div>{items.map(x=><div className="tr" key={x.id}><span>{x.complaint_id}</span><span>{x.customer_name}</span><span>{x.product_name}</span><span>{x.initial_severity}</span><span>{x.status}</span></div>)}</div></div>}
