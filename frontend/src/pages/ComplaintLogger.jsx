import {useDispatch,useSelector} from 'react-redux'; import {submitComplaint,triggerExtraction} from '../store/slices/complaintSlice'; import ComplaintForm from '../components/complaint/ComplaintForm'; import AIIntakePanel from '../components/ai/AIIntakePanel';
export default function ComplaintLogger(){const dispatch=useDispatch();const c=useSelector(s=>s.complaint);const run=(text,name='pasted.txt')=>dispatch(triggerExtraction({file_content:text,file_type:name.split('.').pop(),file_name:name}));const file=async(f)=>run(await f.text(),f.name);return <div className="workspace"><ComplaintForm onSave={data=>dispatch(submitComplaint(data))} savedId={c.savedComplaintId}/><AIIntakePanel complaintId={c.savedComplaintId} onFile={file} onPaste={v=>v.length>40&&run(v)} progress={c.extractionProgress} message={c.extractionMessage} metadata={c.aiMetadata} missing={c.missingFields}/></div>}
